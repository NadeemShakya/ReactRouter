import React, {Component} from 'react'
import {NavLink } from 'react-router-dom';

export default class Settings extends Component {
    render() {
        return(
            <div>
                <h3>Settings</h3>
                <span>Welcome, this is the settings section.</span>

            </div>
        );
    }
}